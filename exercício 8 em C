#include <stdio.h>
int main() {
int i, n1, n2;
printf(" diga o valor 1:");
scanf("%d", &n1);
printf(" diga o valor 2:");
scanf("%d", &n2);
printf("os números são:");
for(i=n1+1; i<n2; i=i+1){
    printf("%d ", i); 
    if (i!= n2-1){printf(",");}
    }
}
