#include <stdio.h>

int main() {
   int num;
   int soma=0;
   do {
    printf("escreva um número:");
   scanf("%d",&num);
   soma=soma+ num;} while (num!=0);
   printf("a soma dos valores é:%d",soma);
   
}
