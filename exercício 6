#include <stdio.h>

int main() {
  int n, i;
  int soma=0;
  printf("digite um número n para fazer o somatório:");
  scanf("%d",&n);
  for(i=0;i<=n; i=i+1){soma= soma+ i*i;}
  printf("a soma é %d:",soma);
  
}
